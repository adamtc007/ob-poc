# Build Determinism Audit

Status: deferred by Gate A replan.

Evidence collected:

- Static inventory was generated by `run_audit_inventory.sh`.
- A deterministic projection/build pipeline for ACP envelopes has not started, by plan constraint.

Finding:

Byte-equality rebuild evidence is currently missing. Gate A cannot be signed off as complete until either:

- an existing deterministic pack generation command is identified and run twice with byte comparison, or
- the Gate A reviewer explicitly accepts that byte-equality testing moves to the first Gate D build-pipeline spike.

Replan decision:

- Gate A1 planning may proceed without byte-equality evidence.
- Production envelope generation/signing may not proceed until byte-equality evidence exists.
- The deterministic build spike must identify the command, output path, and stable hash/byte comparison method.

Recommended Gate B blocker:

Do not approve production envelope generation until a command exists that emits the same pack/envelope artefacts twice from identical inputs and compares bytes or stable hashes.
