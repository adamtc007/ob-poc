# Slice 1 Acceptance Threshold

Frozen before implementation:

- Fixture set: `baseline-fixtures-v1.md`
- Measurement schema: `baseline-measurement-schema.md`

Threshold:

- At least 70% gap closure for `pack_hit`, `verb_hit`, and `first_pass_valid_dsl_draft`.
- Zero invented verbs and zero invented macros on the frozen Slice 1 fixtures.
- Zero prose-only failures for `onboarding-request`, `cbu-maintenance`, and `product-service-taxonomy`.
- All refusal-required fixtures must produce structured refusals with concrete pack/policy reasons.
- All pending-question fixtures must ask only for missing required bindings or disambiguation needed to proceed.

