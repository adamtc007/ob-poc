# Update number attribute macro
s/macro_rules! define_number_attribute {/&/
/^    (/,/^    ) => {/ {
    s/id = \$id:expr,/id = $id:expr,\n        uuid = $uuid:expr,/
}
/const ID: &'static str = \$id;/a\
            const UUID_STR: &'static str = $uuid;

# Update integer attribute macro
/^macro_rules! define_integer_attribute {/,/^}$/ {
    s/id = \$id:expr,/id = $id:expr,\n        uuid = $uuid:expr,/
    /const ID: &'static str = \$id;/a\
            const UUID_STR: &'static str = $uuid;
}

# Update boolean attribute macro
/^macro_rules! define_boolean_attribute {/,/^}$/ {
    s/id = \$id:expr,/id = $id:expr,\n        uuid = $uuid:expr,/
    /const ID: &'static str = \$id;/a\
            const UUID_STR: &'static str = $uuid;
}

# Update date attribute macro
/^macro_rules! define_date_attribute {/,/^}$/ {
    s/id = \$id:expr,/id = $id:expr,\n        uuid = $uuid:expr,/
    /const ID: &'static str = \$id;/a\
            const UUID_STR: &'static str = $uuid;
}
