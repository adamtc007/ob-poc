name: Layering guard

on:
  push:
    branches: [main]
  pull_request:

jobs:
  guard:
    name: ob-poc has no hard-coded path dep on extracted crates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Layering guard
        run: bash scripts/check-layering.sh
