# Quick Test Scenario
# Minimal scenario for testing the animation system

name: "Quick Test"
description: "Simple 2-step test scenario"
typing_speed_ms: 0  # Instant typing for fast testing
pause_after_ms: 500

steps:
  - prompt: "Create a test fund called Demo Fund in Luxembourg"
    expect_verbs:
      - cbu.ensure

  - prompt: "Add a director named Test Person"
    expect_verbs:
      - entity.create-proper-person
      - cbu.assign-role
