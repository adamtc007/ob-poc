//! Journey-pack manifest types relocated from ob-poc's `src/journey/` for
//! Phase 3 slice 2d.2. The boundary-tier needs the pack manifest types to
//! project ACP discovery surface; the rest of the journey module (router,
//! playback, template instantiation, pack manager, state) stays in ob-poc.

pub mod pack;
