[package]
name = "sem_os_core"
version = "0.1.0"
edition = "2021"
description = "Semantic OS core — pure domain types, port traits, gate logic. Zero sqlx."

[dependencies]
async-trait = "0.1"
thiserror = "1.0"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
hex = "0.4"
prost = "0.13"
serde_yaml = "0.9"
sqlparser = "0.53"
tracing = "0.1"
uuid = { version = "1", features = ["v4", "v5", "serde"] }
chrono = { version = "0.4", features = ["serde"] }

[build-dependencies]
prost-build = "0.13"
