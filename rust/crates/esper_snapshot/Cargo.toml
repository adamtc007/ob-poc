[package]
name = "esper_snapshot"
version = "0.1.0"
edition = "2021"
description = "Wire format and ABI for ESPER navigation snapshots"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
thiserror = "1.0"

[dev-dependencies]
proptest = "1.4"
criterion = "0.5"

[[bench]]
name = "grid_query"
harness = false
