[package]
name = "sem_os_obpoc_adapter"
version = "0.1.0"
edition = "2021"
description = "Semantic OS ob-poc adapter — scanner, seed builder, config bridge."

[dependencies]
sem_os_core = { path = "../sem_os_core" }
dsl-core = { path = "../dsl-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
anyhow = "1.0"
tracing = "0.1"
