[package]
name = "dsl-atoms"
version = "0.1.0"
edition = "2021"
rust-version = "1.95"
description = "Shared atom kind taxonomy for the unified DSL v0.1"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[lints.rust]
unreachable_pub = "deny"
dead_code = "deny"
