[package]
name = "ob-execution-types"
version = "0.1.0"
edition = "2021"
description = "Core execution types for ob-poc DSL execution layer"
license = "MIT"

[dependencies]
uuid = { version = "1.11", features = ["v4", "serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]

[features]
default = []
