[package]
name = "esper_core"
version = "0.1.0"
edition = "2021"
description = "Navigation engine for ESPER - verbs, state machine, and effects"
license = "MIT"

[dependencies]
esper_snapshot = { path = "../esper_snapshot" }
bitflags = "2.4"
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
proptest = "1.4"
