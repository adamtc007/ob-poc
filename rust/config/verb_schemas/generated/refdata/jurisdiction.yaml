version: '2.0'
domain: jurisdiction
description: Jurisdiction reference data (US, GB, LU, etc.)
verbs:
- verb: jurisdiction.read
  domain: jurisdiction
  action: read
  aliases:
  - fetch
  - get
  - read
  args:
    style: keyworded
    required:
      code:
        type: str
  positional_sugar:
  - code
  invocation_phrases:
  - read jurisdiction
  - read the jurisdiction
  - get jurisdiction
  - fetch jurisdiction
  - read country
  - jurisdiction read
  examples:
  - (jurisdiction.read :code "...")
  doc: Read a jurisdiction by code
  tier: reference
- verb: jurisdiction.ensure
  domain: jurisdiction
  action: ensure
  aliases:
  - ensure
  args:
    style: keyworded
    required:
      code:
        type: str
      name:
        type: str
      country-code:
        type: str
    optional:
      offshore:
        type: bool
      region:
        type: str
      entity-formation-allowed:
        type: bool
      regulatory-authority:
        type: str
      regulatory-framework:
        type: str
  positional_sugar:
  - code
  - name
  invocation_phrases:
  - ensure jurisdiction
  - ensure the jurisdiction
  - ensure country
  - jurisdiction ensure
  examples:
  - (jurisdiction.ensure :code "..." :name "..." :country-code "...")
  doc: Create or update a jurisdiction
  tier: intent
- verb: jurisdiction.delete
  domain: jurisdiction
  action: delete
  aliases:
  - delete
  - drop
  - remove
  args:
    style: keyworded
    required:
      code:
        type: str
  positional_sugar:
  - code
  invocation_phrases:
  - delete jurisdiction
  - delete the jurisdiction
  - remove jurisdiction
  - drop jurisdiction
  - delete country
  - jurisdiction delete
  examples:
  - (jurisdiction.delete :code "...")
  doc: Delete a jurisdiction
  tier: intent
- verb: jurisdiction.list
  domain: jurisdiction
  action: list
  aliases:
  - get all
  - list
  - show
  args:
    style: keyworded
    optional:
      region:
        type: str
      offshore:
        type: bool
  invocation_phrases:
  - list jurisdiction
  - list the jurisdiction
  - show jurisdiction
  - get all jurisdiction
  - list country
  - jurisdiction list
  examples:
  - (jurisdiction.list)
  doc: List all jurisdictions
  tier: reference
