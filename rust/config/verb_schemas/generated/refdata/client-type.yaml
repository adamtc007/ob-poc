version: '2.0'
domain: client-type
description: Client type reference data (FUND, CORPORATE, etc.)
verbs:
- verb: client-type.deactivate
  domain: client-type
  action: deactivate
  aliases:
  - deactivate
  - disable
  - stop
  args:
    style: keyworded
    required:
      code:
        type: str
    optional:
      is-active:
        type: bool
  positional_sugar:
  - code
  invocation_phrases:
  - deactivate client-type
  - deactivate the client-type
  - disable client-type
  - stop client-type
  - client-type deactivate
  examples:
  - (client-type.deactivate :code "...")
  doc: Deactivate a client type
  tier: intent
- verb: client-type.ensure
  domain: client-type
  action: ensure
  aliases:
  - ensure
  args:
    style: keyworded
    required:
      code:
        type: str
      name:
        type: str
    optional:
      description:
        type: str
      display-order:
        type: int
      is-active:
        type: bool
  positional_sugar:
  - code
  - name
  invocation_phrases:
  - ensure client-type
  - ensure the client-type
  - client-type ensure
  examples:
  - (client-type.ensure :code "..." :name "...")
  doc: Create or update a client type
  tier: intent
- verb: client-type.read
  domain: client-type
  action: read
  aliases:
  - fetch
  - get
  - read
  args:
    style: keyworded
    required:
      code:
        type: str
  positional_sugar:
  - code
  invocation_phrases:
  - read client-type
  - read the client-type
  - get client-type
  - fetch client-type
  - client-type read
  examples:
  - (client-type.read :code "...")
  doc: Read a client type by code
  tier: reference
- verb: client-type.list
  domain: client-type
  action: list
  aliases:
  - get all
  - list
  - show
  args:
    style: keyworded
    optional:
      is-active:
        type: bool
  invocation_phrases:
  - list client-type
  - list the client-type
  - show client-type
  - get all client-type
  - client-type list
  examples:
  - (client-type.list)
  doc: List all client types
  tier: reference
