version: '2.0'
domain: screening
description: Entity screening operations (PEP, sanctions, adverse media)
verbs:
- verb: screening.adverse-media
  domain: screening
  action: adverse-media
  aliases:
  - adverse-media
  args:
    style: keyworded
    required:
      entity-id:
        type: entity_name
    optional:
      lookback-months:
        type: int
  positional_sugar:
  - entity-id
  invocation_phrases:
  - run adverse media check
  - adverse media screening
  - check for negative news
  - media scan
  - screen for adverse media
  - negative media check
  - run media screening
  - check news coverage
  examples:
  - (screening.adverse-media :entity-id "...")
  doc: Run adverse media screening
  tier: intent
- verb: screening.sanctions
  domain: screening
  action: sanctions
  aliases:
  - sanctions
  args:
    style: keyworded
    required:
      entity-id:
        type: entity_name
    optional:
      lists:
        type: list
  positional_sugar:
  - entity-id
  invocation_phrases:
  - run sanctions check
  - sanctions screening
  - check sanctions lists
  - screen against sanctions
  - OFAC check
  - sanctions scan
  - run sanctions screening
  - check if sanctioned
  examples:
  - (screening.sanctions :entity-id "...")
  doc: Run sanctions list screening
  tier: intent
- verb: screening.pep
  domain: screening
  action: pep
  aliases:
  - pep
  args:
    style: keyworded
    required:
      entity-id:
        type: entity_name
    optional:
      provider:
        type: str
  positional_sugar:
  - entity-id
  invocation_phrases:
  - run PEP screening
  - check for PEP
  - PEP check
  - screen for politically exposed
  - is this person a PEP
  - PEP scan
  - check political exposure
  - run PEP check
  examples:
  - (screening.pep :entity-id "...")
  doc: Run PEP (Politically Exposed Persons) screening
  tier: intent
