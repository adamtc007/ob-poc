fqn: family.registry_governance
family_id: registry_governance
label: Registry Governance
description: Constellation family for SemOS stewardship — changeset authoring, governance pipeline, registry queries, attribute lifecycle.
domain_id: semos
selection_rules:
  - condition: "true"
    target_constellation: registry.stewardship
    priority: 30
constellation_refs:
  - constellation_id: registry.stewardship
    label: SemOS Stewardship
    jurisdiction: ALL
    entity_kind: registry
    triggers:
      - changeset
      - governance
      - registry
      - maintenance
      - attribute
      - schema
      - stewardship
candidate_jurisdictions: [ALL]
candidate_entity_kinds: [registry]
grounding_threshold:
  required_input_keys: []
  requires_entity_instance: false
  allows_draft_instance: true
