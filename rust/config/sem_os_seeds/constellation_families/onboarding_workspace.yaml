fqn: family.onboarding_workspace
family_id: onboarding_workspace
label: OnBoarding Workspace
description: Workspace-level root for OnBoarding orchestration.
domain_id: onboarding
selection_rules:
  - condition: "true"
    target_constellation: onboarding.workspace
    priority: 60
constellation_refs:
  - constellation_id: onboarding.workspace
    label: OnBoarding Workspace Root
    jurisdiction: ALL
    entity_kind: cbu
    triggers: [onboarding, provisioning, readiness, pipeline]
candidate_jurisdictions: [ALL]
candidate_entity_kinds: [cbu]
grounding_threshold:
  required_input_keys: []
  requires_entity_instance: false
  allows_draft_instance: true
