fqn: family.deal_workspace
family_id: deal_workspace
label: Deal Workspace
description: Workspace-level root for Deal management.
domain_id: deal
selection_rules:
  - condition: "true"
    target_constellation: deal.workspace
    priority: 60
constellation_refs:
  - constellation_id: deal.workspace
    label: Deal Workspace Root
    jurisdiction: ALL
    entity_kind: deal
    triggers: [deal, pricing, contract, billing, client-principal]
candidate_jurisdictions: [ALL]
candidate_entity_kinds: [deal]
grounding_threshold:
  required_input_keys: []
  requires_entity_instance: false
  allows_draft_instance: true
