fqn: family.instrument_workspace
family_id: instrument_workspace
label: Instrument Matrix Workspace
description: Workspace-level root for InstrumentMatrix — trading, settlement, custody.
domain_id: trading
selection_rules:
  - condition: "true"
    target_constellation: instrument.workspace
    priority: 60
constellation_refs:
  - constellation_id: instrument.workspace
    label: Instrument Matrix Workspace Root
    jurisdiction: ALL
    entity_kind: matrix
    triggers: [trading, settlement, custody, instrument, gateway, movement, isda]
candidate_jurisdictions: [ALL]
candidate_entity_kinds: [matrix, cbu]
grounding_threshold:
  required_input_keys: []
  requires_entity_instance: false
  allows_draft_instance: true
