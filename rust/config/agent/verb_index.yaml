# Verb Index for RAG Discovery
#
# This file provides a semantic index for discovering DSL verbs based on
# user intent, graph context, and workflow state. It enables the agent to:
#
# 1. Find relevant verbs for a given intent (semantic search)
# 2. Suggest verbs based on cursor position in the graph
# 3. Filter verbs by workflow phase or lifecycle stage
# 4. Provide contextual examples for each verb
#
# Structure:
#   - intents: Maps natural language intents to verb patterns
#   - categories: Groups verbs by functional category
#   - graph_context: Maps graph positions to relevant verbs
#   - workflow_phases: Maps KYC workflow phases to applicable verbs

version: "1.0"

# =============================================================================
# INTENT PATTERNS
# =============================================================================
# Maps natural language intent patterns to verbs. Used for semantic search.
# Each intent has:
#   - patterns: Natural language phrases that indicate this intent
#   - verbs: List of verbs that can fulfill this intent
#   - priority: Higher = more likely to be suggested first

intents:
  # ---------------------------------------------------------------------------
  # ENTITY MANAGEMENT INTENTS
  # ---------------------------------------------------------------------------
  create_entity:
    patterns:
      - "create a person"
      - "add a new entity"
      - "create company"
      - "add director"
      - "create fund"
      - "new trust"
      - "add partnership"
    verbs:
      - entity.create
      - entity.create
      - entity.create
      - entity.create
      - entity.ensure
      - entity.ensure
    priority: 100

  find_entity:
    patterns:
      - "find entity"
      - "search for person"
      - "lookup company"
      - "who is"
      - "show entity"
    verbs:
      - entity.read
      - entity.list
    priority: 90

  # ---------------------------------------------------------------------------
  # CBU MANAGEMENT INTENTS
  # ---------------------------------------------------------------------------
  create_cbu:
    patterns:
      - "create client"
      - "new cbu"
      - "create business unit"
      - "add new client"
    verbs:
      - cbu.create
      - cbu.ensure
    priority: 100

  request_onboarding:
    patterns:
      - "onboarding request"
      - "request onboarding for this deal"
      - "submit onboarding handoff"
      - "handoff this deal to ops"
      - "product onboarding"
      - "deal onboarding"
      - "batch onboarding request"
    verbs:
      - deal.request-onboarding
      - deal.request-onboarding-batch
    priority: 99

  assign_role:
    patterns:
      - "assign role"
      - "add as director"
      - "make signatory"
      - "set as ubo"
      - "add role"
      - "link entity to cbu"
    verbs:
      - cbu.assign-role
    priority: 95

  remove_role:
    patterns:
      - "remove role"
      - "unassign"
      - "revoke role"
      - "remove from cbu"
    verbs:
      - cbu.remove-role
    priority: 85

  view_cbu:
    patterns:
      - "show cbu"
      - "view client"
      - "display structure"
      - "show parties"
      - "who are the parties"
    verbs:
      - cbu.show
      - cbu.parties
      - cbu.read
    priority: 90

  # ---------------------------------------------------------------------------
  # OWNERSHIP & UBO INTENTS
  # ---------------------------------------------------------------------------
  add_ownership:
    patterns:
      - "add ownership"
      - "owns"
      - "shareholder"
      - "add owner"
      - "ownership stake"
      - "percentage ownership"
    verbs:
      - ubo.add-ownership
      - ubo.update-ownership
    priority: 95

  trace_ubo:
    patterns:
      - "trace ubo"
      - "find beneficial owner"
      - "who owns"
      - "ownership chain"
      - "ultimate owner"
      - "trace ownership"
    verbs:
      - ubo.trace-chains
      - ubo.infer-chain
      - ubo.list-ubos
    priority: 90

  register_ubo:
    patterns:
      - "register ubo"
      - "mark as ubo"
      - "beneficial owner determination"
    verbs:
      - ubo.register-ubo
      - ubo.verify-ubo
    priority: 85

  # ---------------------------------------------------------------------------
  # DOCUMENT INTENTS
  # ---------------------------------------------------------------------------
  add_document:
    patterns:
      - "add document"
      - "upload document"
      - "catalog document"
      - "attach passport"
      - "add certificate"
      - "submit document"
    verbs:
      - document.catalog
    priority: 95

  extract_document:
    patterns:
      - "extract from document"
      - "read document"
      - "parse document"
      - "get data from document"
    verbs:
      - document.extract
      - document.extract-to-observations
    priority: 85

  request_document:
    patterns:
      - "request document"
      - "ask for document"
      - "document request"
      - "need document"
    verbs:
      - document.create-request
      - rfi.generate
    priority: 90

  # ---------------------------------------------------------------------------
  # KYC & SCREENING INTENTS
  # ---------------------------------------------------------------------------
  create_case:
    patterns:
      - "create kyc case"
      - "start investigation"
      - "new case"
      - "initiate review"
      - "open case"
      - "open delta kyc case"
      - "start cbu delta review"
    verbs:
      - kyc-case.create
    priority: 100

  run_screening:
    patterns:
      - "run screening"
      - "check sanctions"
      - "pep check"
      - "screen entity"
      - "adverse media"
      - "sanctions check"
    verbs:
      - screening.run
      - screening.pep
      - screening.sanctions
      - screening.adverse-media
    priority: 95

  update_case:
    patterns:
      - "update case"
      - "case status"
      - "escalate case"
      - "approve case"
      - "reject case"
      - "withdraw case"
      - "close case"
    verbs:
      - kyc-case.close
      - kyc-case.update-status
      - kyc-case.escalate
    priority: 85

  # ---------------------------------------------------------------------------
  # GRAPH NAVIGATION INTENTS
  # ---------------------------------------------------------------------------
  view_graph:
    patterns:
      - "show graph"
      - "visualize"
      - "view structure"
      - "display network"
      - "entity map"
    verbs:
      - graph.view
      - graph.focus
    priority: 90

  filter_graph:
    patterns:
      - "filter graph"
      - "show only"
      - "filter by role"
      - "filter by jurisdiction"
    verbs:
      - graph.filter
      - graph.group-by
    priority: 85

  find_path:
    patterns:
      - "find path"
      - "how connected"
      - "relationship between"
      - "trace connection"
    verbs:
      - graph.path
      - graph.find-connected
      - graph.ancestors
      - graph.descendants
    priority: 80

  # ---------------------------------------------------------------------------
  # CUSTODY & TRADING INTENTS
  # ---------------------------------------------------------------------------
  setup_custody:
    patterns:
      - "setup custody"
      - "add trading universe"
      - "configure settlement"
      - "create ssi"
      - "add booking rule"
    verbs:
      - cbu-custody.add-universe
      - cbu-custody.create-ssi
      - cbu-custody.add-booking-rule
    priority: 85

  isda_setup:
    patterns:
      - "create isda"
      - "add csa"
      - "derivative agreement"
      - "otc setup"
    verbs:
      - isda.create
      - isda.add-csa
      - isda.add-coverage
    priority: 80

  # ---------------------------------------------------------------------------
  # TRADING MATRIX INTENTS
  # ---------------------------------------------------------------------------
  # The Trading Matrix consists of: IM Assignment, Pricing Config, SLA, Cash Sweep
  # Together they define WHO executes trades, HOW pricing is sourced, WHAT service
  # levels are committed, and HOW cash is managed for a given instrument/market scope.

  assign_investment_manager:
    patterns:
      - "assign investment manager"
      - "add IM"
      - "link manager to fund"
      - "set up IM assignment"
      - "who trades for this fund"
      - "assign manager"
      - "configure IM"
      - "set trading delegation"
      - "who executes trades"
    verbs:
      - investment-manager.assign
      - investment-manager.set-scope
      - investment-manager.link-connectivity
    context: |
      IM assignments define WHO executes trades for a CBU. Each assignment has:
      - Scope: which instruments/markets/currencies they trade
      - Priority: lower = more specific, used for routing
      - Instruction method: SWIFT, FIX, CTM, etc.
      - Permissions: can_trade, can_settle, can_affirm
    priority: 90

  find_im_for_trade:
    patterns:
      - "which IM handles"
      - "who trades equities"
      - "find manager for"
      - "route trade to"
      - "IM routing"
      - "trading delegation lookup"
    verbs:
      - investment-manager.find-for-trade
      - investment-manager.list
    context: |
      Use find-for-trade to discover which IM assignment covers a specific
      trade's characteristics (instrument class, market, currency, OTC asset class).
      Returns the matching IM with instruction method for order routing.
    priority: 85

  configure_pricing:
    patterns:
      - "set pricing source"
      - "configure pricing"
      - "add pricing config"
      - "where do we get prices"
      - "pricing feed"
      - "set up bloomberg pricing"
      - "configure reuters"
      - "valuation source"
      - "NAV pricing"
    verbs:
      - pricing-config.set
      - pricing-config.link-resource
      - pricing-config.find-for-instrument
    context: |
      Pricing configs define WHERE prices come from for NAV calculation.
      Key attributes:
      - Source: BLOOMBERG, REUTERS, MARKIT, REFINITIV, ICE, MODEL, INTERNAL
      - Price type: CLOSING, MID, BID, ASK, VWAP, OFFICIAL
      - Fallback: what to use if primary stale
      - Tolerance: acceptable variance before flagging
      - Stale action: WARN, BLOCK, USE_FALLBACK, ESCALATE
    priority: 85

  setup_sla:
    patterns:
      - "add sla"
      - "set service level"
      - "configure sla"
      - "sla commitment"
      - "service level agreement"
      - "commit to sla"
      - "settlement sla"
      - "reporting sla"
    verbs:
      - sla.commit
      - sla.bind
      - sla.list-templates
    context: |
      SLA commitments define WHAT service levels are promised to a client.
      Templates exist for common SLAs (T+1 settlement, 99.9% uptime, etc.).
      Commitments can be scoped to specific instruments/markets/currencies.
      Targets can be overridden from template defaults per CBU.
    priority: 80

  track_sla:
    patterns:
      - "record sla measurement"
      - "track sla"
      - "sla breach"
      - "report breach"
      - "sla status"
      - "are we meeting sla"
      - "sla performance"
    verbs:
      - sla.record-measurement
      - sla.report-breach
      - sla.list-open-breaches
      - sla.list-measurements
    context: |
      SLA tracking involves periodic measurements against committed targets.
      Measurements can be MET, WARNING, or BREACH status.
      Breaches require root cause categorization and remediation planning.
      Common categories: SYSTEM, VENDOR, MARKET, CLIENT, INTERNAL, EXTERNAL
    priority: 75

  configure_cash_sweep:
    patterns:
      - "set up cash sweep"
      - "configure sweep"
      - "cash management"
      - "stif setup"
      - "overnight investment"
      - "money market fund"
      - "idle cash"
      - "sweep threshold"
    verbs:
      - cash-sweep.configure
      - cash-sweep.update-threshold
      - cash-sweep.change-vehicle
    context: |
      Cash sweep configs define HOW idle cash is invested per currency.
      Vehicle types: STIF, MMF, DEPOSIT, OVERNIGHT_REPO, TRI_PARTY_REPO, MANUAL
      Key settings:
      - Threshold: minimum balance before sweep
      - Time/timezone: when sweep runs
      - Frequency: INTRADAY, DAILY, WEEKLY, MONTHLY
      - Interest allocation: ACCRUED, MONTHLY, QUARTERLY, REINVEST
    priority: 75

  # ---------------------------------------------------------------------------
  # LIFECYCLE & GAP ANALYSIS INTENTS
  # ---------------------------------------------------------------------------
  analyze_lifecycle_gaps:
    patterns:
      - "what's missing"
      - "analyze gaps"
      - "readiness check"
      - "can we trade"
      - "what do we need to provision"
      - "lifecycle gaps"
      - "missing resources"
    verbs:
      - service-resource.analyze-lifecycle-gaps
      - service-resource.check-lifecycle-readiness
      - service-resource.discover-lifecycles
      - matrix-overlay.unified-gaps
    context: |
      Gap analysis compares what a CBU WANTS to trade (universe) against
      what they HAVE provisioned. Identifies missing lifecycles, resources,
      SSIs, and agreements needed before trading can begin.
    priority: 85

  provision_lifecycle:
    patterns:
      - "provision resources"
      - "set up for trading"
      - "provision lifecycle"
      - "enable trading"
      - "configure for"
    verbs:
      - service-resource.provision-lifecycle
      - service-resource.activate-lifecycle
      - service-resource.generate-lifecycle-plan
      - service-resource.execute-lifecycle-plan
    context: |
      Lifecycle provisioning creates the operational infrastructure for trading:
      accounts, SSIs, connectivity, data feeds, agreements. Uses dependency
      graph to ensure correct ordering.
    priority: 80

  # ---------------------------------------------------------------------------
  # PRODUCT OVERLAY INTENTS
  # ---------------------------------------------------------------------------
  subscribe_product:
    patterns:
      - "subscribe to product"
      - "add product subscription"
      - "enable product"
      - "turn on custody"
      - "add prime brokerage"
    verbs:
      - cbu.add-product
      - cbu.list-subscriptions
    context: |
      Product subscriptions link CBUs to products like Custody, Prime Brokerage,
      Fund Accounting. Products ADD capabilities to the trading matrix.
    priority: 85

  configure_product_overlay:
    patterns:
      - "add product overlay"
      - "configure product for instruments"
      - "what products apply"
      - "product scope"
    verbs:
      - matrix-overlay.add
      - matrix-overlay.effective-matrix
      - matrix-overlay.compare-products
    context: |
      Product overlays define how products apply to specific instrument/market
      combinations. They ADD services, SLAs, and resources to the base matrix.
      Use effective-matrix to see the combined result.
    priority: 75

  # ---------------------------------------------------------------------------
  # GLEIF RESEARCH INTENTS
  # ---------------------------------------------------------------------------
  # Single deterministic lookups - each verb does ONE thing reliably.
  # NOTE: SICAVs are self-governed umbrella-level structures. They have NO
  # umbrella above them, so get-umbrella will return null. For SICAVs, use
  # get-manager to find the ManCo, then trace-ownership from the ManCo.

  gleif_research:
    patterns:
      - "research entity"
      - "lookup lei"
      - "find in gleif"
      - "gleif search"
      - "external registry"
      - "check lei"
    verbs:
      - gleif.search
      - gleif.get-record
      - gleif.enrich
    priority: 90

  gleif_fund_structure:
    patterns:
      - "find umbrella"
      - "get umbrella fund"
      - "which umbrella"
      - "subfund of"
      - "parent fund"
      - "sicav structure"
    verbs:
      - gleif.get-umbrella
      - gleif.get-manager
    context: |
      SICAVs are self-governed umbrella structures with NO parent above them.
      If get-umbrella returns null, try get-manager to find the ManCo, then
      trace-ownership from there to find natural person UBOs.
    priority: 85

  gleif_fund_manager:
    patterns:
      - "find manager"
      - "who manages fund"
      - "fund manager"
      - "manco"
      - "aifm"
      - "investment manager for fund"
    verbs:
      - gleif.get-manager
      - gleif.get-managed-funds
    context: |
      IS_FUND-MANAGED_BY relationship. For SICAVs, this is the starting point
      for ownership tracing since they have no umbrella above them.
    priority: 85

  gleif_master_feeder:
    patterns:
      - "master fund"
      - "feeder fund"
      - "master-feeder"
      - "invests into"
      - "feeder structure"
    verbs:
      - gleif.get-master-fund
    priority: 80

  gleif_isin_lookup:
    patterns:
      - "lookup isin"
      - "isin to lei"
      - "find by isin"
      - "security issuer"
      - "who issued"
    verbs:
      - gleif.lookup-by-isin
    priority: 80

  gleif_ownership_trace:
    patterns:
      - "trace ownership"
      - "ownership chain"
      - "who owns"
      - "parent company"
      - "corporate structure"
      - "ultimate parent"
    verbs:
      - gleif.trace-ownership
      - gleif.get-parent
      - gleif.resolve-successor
    priority: 90

  # ---------------------------------------------------------------------------
  # PRODUCT & SERVICE INTENTS
  # ---------------------------------------------------------------------------
  add_product:
    patterns:
      - "add product"
      - "subscribe product"
      - "onboard to product"
      - "attach product"
      - "enable service"
      - "subscribe to"
      - "provision"
    verbs:
      - cbu.add-product
      - service-resource.provision
    priority: 85

  product_catalog:
    patterns:
      - "product catalog"
      - "show products"
      - "list products"
      - "show services for product"
      - "list services for product"
      - "show resources for service"
      - "list resource types"
      - "show resource dictionary"
      - "list attributes for resource"
    verbs:
      - product.read
      - product.list
      - service.read
      - service.list
      - service.list-by-product
      - service-resource.read
      - service-resource.list
      - service-resource.list-by-service
      - service-resource.list-attributes
      - attribute.read
      - attribute.list
    priority: 82

  configure_service:
    patterns:
      - "configure service"
      - "set attribute"
      - "activate resource"
    verbs:
      - service-resource.set-attr
      - service-resource.activate
    priority: 80

# =============================================================================
# VERB CATEGORIES
# =============================================================================
# Groups verbs by functional category for browsing and filtering

categories:
  entity_management:
    label: "Entity Management"
    description: "Create, update, and manage legal entities"
    verbs:
      - entity.create
      - entity.create
      - entity.create
      - entity.create
      - entity.ensure
      - entity.ensure
      - entity.read
      - entity.list
      - entity.update
      - entity.delete

  cbu_operations:
    label: "CBU Operations"
    description: "Client Business Unit lifecycle and role management"
    verbs:
      - cbu.create
      - cbu.ensure
      - cbu.read
      - cbu.rename
      - cbu.set-jurisdiction
      - cbu.set-client-type
      - cbu.set-commercial-client
      - cbu.set-category
      - cbu.submit-for-validation
      - cbu.request-proof-update
      - cbu.reopen-validation
      - cbu.delete
      - cbu.assign-role
      - cbu.remove-role
      - cbu.parties
      - cbu.show
      - cbu.add-product
      - cbu.decide

  ownership_control:
    label: "Ownership & Control"
    description: "Ownership chains, UBO determination, and control relationships"
    verbs:
      - ubo.add-ownership
      - ubo.update-ownership
      - ubo.end-relationship
      - ubo.list-owners
      - ubo.list-owned
      - ubo.trace-chains
      - ubo.infer-chain
      - ubo.register-ubo
      - ubo.verify-ubo
      - ubo.list-ubos
      - control.add-control
      - control.list-controllers

  document_management:
    label: "Document Management"
    description: "Document catalog, extraction, and requests"
    verbs:
      - document.catalog
      - document.extract
      - document.extract-to-observations
      - document.create-request
      - document.receive-request
      - document.verify-request
      - rfi.generate
      - rfi.check-completion

  kyc_workflow:
    label: "KYC Workflow"
    description: "KYC case management, workstreams, and approvals"
    verbs:
      - kyc-case.create
      - kyc-case.update-status
      - kyc-case.escalate
      - kyc-case.close
      - kyc-case.assign
      - kyc-case.state
      - entity-workstream.create
      - entity-workstream.update-status
      - entity-workstream.complete
      - red-flag.raise
      - red-flag.mitigate

  screening:
    label: "Screening"
    description: "Sanctions, PEP, and adverse media screening"
    verbs:
      - screening.run
      - screening.complete
      - screening.review-hit
      - screening.pep
      - screening.sanctions
      - screening.adverse-media

  graph_visualization:
    label: "Graph Visualization"
    description: "View and navigate entity graphs"
    verbs:
      - graph.view
      - graph.focus
      - graph.filter
      - graph.group-by
      - graph.path
      - graph.find-connected
      - graph.ancestors
      - graph.descendants
      - graph.compare

  custody_settlement:
    label: "Custody & Settlement"
    description: "Trading universe, SSIs, and booking rules"
    verbs:
      - cbu-custody.add-universe
      - cbu-custody.create-ssi
      - cbu-custody.activate-ssi
      - cbu-custody.add-booking-rule
      - cbu-custody.lookup-ssi
      - cbu-custody.validate-booking-coverage
      - isda.create
      - isda.add-csa
      - isda.add-coverage

  product_maintenance:
    label: "Product Maintenance"
    description: "Design-time product, service, servicing-resource, and resource-dictionary taxonomy"
    verbs:
      - product.read
      - product.list
      - service.read
      - service.list
      - service.list-by-product
      - service-resource.read
      - service-resource.list
      - service-resource.list-by-service
      - service-resource.list-attributes
      - attribute.read
      - attribute.list

  service_activation:
    label: "Service Activation"
    description: "Runtime activation of service resources for an onboarded CBU"
    verbs:
      - cbu.add-product
      - cbu.remove-product
      - service-resource.provision
      - service-resource.set-attr
      - service-resource.activate
      - service-resource.suspend
      - delivery.record
      - delivery.complete

  fund_structure:
    label: "Fund Structure"
    description: "Fund hierarchy, share classes, and investor registry"
    verbs:
      - fund.ensure
      - share-class.create
      - share-class.update-nav
      - holding.create
      - movement.subscribe
      - movement.redeem

  gleif_research:
    label: "GLEIF Research"
    description: "External registry lookups for entity verification and fund structure discovery. SICAVs have no umbrella - use get-manager to find ManCo."
    verbs:
      - gleif.search
      - gleif.get-record
      - gleif.enrich
      - gleif.get-umbrella
      - gleif.get-manager
      - gleif.get-master-fund
      - gleif.lookup-by-isin
      - gleif.trace-ownership
      - gleif.get-parent
      - gleif.get-children
      - gleif.resolve-successor

  trading_matrix:
    label: "Trading Matrix"
    description: "Investment Manager assignment, pricing sources, SLA commitments, and cash management - the operational backbone for trading"
    verbs:
      # Investment Manager
      - investment-manager.assign
      - investment-manager.set-scope
      - investment-manager.link-connectivity
      - investment-manager.list
      - investment-manager.suspend
      - investment-manager.terminate
      - investment-manager.find-for-trade
      # Pricing
      - pricing-config.set
      - pricing-config.link-resource
      - pricing-config.list
      - pricing-config.find-for-instrument
      # SLA
      - sla.commit
      - sla.bind
      - sla.bind
      - sla.override-target
      - sla.list-commitments
      - sla.record-measurement
      - sla.report-breach
      - sla.list-open-breaches
      # Cash Sweep
      - cash-sweep.configure
      - cash-sweep.update-threshold
      - cash-sweep.change-vehicle
      - cash-sweep.list
      # Lifecycle
      - service-resource.analyze-lifecycle-gaps
      - service-resource.check-lifecycle-readiness
      - service-resource.provision-lifecycle
      - service-resource.generate-lifecycle-plan
      # Product Overlay
      - matrix-overlay.add
      - matrix-overlay.effective-matrix
      - cbu.add-product

# =============================================================================
# GRAPH CONTEXT MAPPINGS
# =============================================================================
# Maps cursor position in the graph to relevant verbs.
# Used to suggest verbs based on what the user is looking at.

graph_context:
  # When cursor is on a CBU node
  cursor_on_cbu:
    verbs:
      - cbu.show
      - cbu.assign-role
      - cbu.add-product
      - cbu.parties
      - kyc-case.create
      - graph.view
      - ubo.trace-chains
    priority: 100

  # When cursor is on an entity (person or company)
  cursor_on_entity:
    verbs:
      - cbu.assign-role
      - ubo.add-ownership
      - document.catalog
      - screening.run
      - graph.focus
      - graph.ancestors
      - entity-workstream.create
    priority: 95

  # When cursor is on a person specifically
  cursor_on_person:
    verbs:
      - cbu.assign-role
      - ubo.register-ubo
      - screening.pep
      - document.catalog
      - allegation.record
    priority: 90

  # When cursor is on an ownership edge
  cursor_on_ownership:
    verbs:
      - ubo.update-ownership
      - ubo.end-relationship
      - ubo.infer-chain
      - graph.ancestors
    priority: 85

  # When cursor is on a product node
  cursor_on_product:
    verbs:
      - product.read
      - service.list-by-product
      - cbu.remove-product
      - cbu.add-product
    priority: 80

  # When viewing UBO layer
  layer_ubo:
    verbs:
      - ubo.trace-chains
      - ubo.register-ubo
      - ubo.verify-ubo
      - ubo.snapshot-cbu
      - graph.ancestors
    priority: 90

  # When viewing KYC layer
  layer_kyc:
    verbs:
      - kyc-case.state
      - entity-workstream.update-status
      - screening.run
      - document.create-request
      - red-flag.raise
    priority: 90

  # When viewing service layer
  layer_services:
    verbs:
      - service.list-by-product
      - service-resource.list-by-service
      - service-resource.provision
      - service-resource.activate
    priority: 85

  # When viewing custody layer
  layer_custody:
    verbs:
      - cbu-custody.add-universe
      - cbu-custody.create-ssi
      - cbu-custody.add-booking-rule
      - isda.create
    priority: 85

# =============================================================================
# WORKFLOW PHASE MAPPINGS
# =============================================================================
# Maps KYC workflow phases to applicable verbs.
# Used to filter verb suggestions based on current workflow state.

workflow_phases:
  intake:
    label: "Intake"
    description: "Initial client data collection"
    verbs:
      - cbu.create
      - cbu.ensure
      - entity.create
      - entity.create
      - cbu.assign-role
      - allegation.record
      - kyc-case.create
    transitions_to:
      - entity_collection

  entity_collection:
    label: "Entity Collection"
    description: "Gathering all related entities and roles"
    verbs:
      - entity.create
      - entity.create
      - cbu.assign-role
      - ubo.add-ownership
      - ubo.trace-chains
      - entity-workstream.create
    required_before_advance:
      - min_directors: 1
      - min_signatories: 1
    transitions_to:
      - screening

  screening:
    label: "Screening"
    description: "Running sanctions, PEP, and adverse media checks"
    verbs:
      - screening.run
      - screening.complete
      - screening.review-hit
      - screening.pep
      - screening.sanctions
    required_before_advance:
      - all_entities_screened: true
    transitions_to:
      - document_collection

  document_collection:
    label: "Document Collection"
    description: "Collecting and verifying required documents"
    verbs:
      - document.create-request
      - document.catalog
      - document.receive-request
      - document.verify-request
      - rfi.generate
      - rfi.check-completion
    required_before_advance:
      - mandatory_docs_verified: true
    transitions_to:
      - ubo_determination

  ubo_determination:
    label: "UBO Determination"
    description: "Calculating and verifying beneficial ownership"
    verbs:
      - ubo.trace-chains
      - ubo.register-ubo
      - ubo.verify-ubo
      - ubo.check-completeness
      - ubo.snapshot-cbu
    required_before_advance:
      - ubo_complete: true
    transitions_to:
      - review

  review:
    label: "Review"
    description: "Final review and decision"
    verbs:
      - kyc-case.state
      - cbu.show
      - red-flag.raise
      - red-flag.mitigate
      - kyc-case.escalate
      - cbu.decide
    transitions_to:
      - approved
      - rejected

  approved:
    label: "Approved"
    description: "Client approved, ready for products"
    verbs:
      - cbu.add-product
      - service-resource.provision
      - cbu-custody.add-universe
      - cbu-custody.create-ssi

  rejected:
    label: "Rejected"
    description: "Client rejected"
    verbs:
      - kyc-case.close

# =============================================================================
# VERB EXAMPLES
# =============================================================================
# Provides contextual examples for verb suggestions.
# Used to help users understand how to use each verb.

examples:
  "cbu.create":
    short: "Create a Luxembourg fund CBU"
    dsl: '(cbu.create :name "Apex Fund" :jurisdiction "LU" :client-type "FUND")'

  "cbu.assign-role":
    short: "Assign John Smith as director"
    dsl: '(cbu.assign-role :cbu-id @fund :entity-id @john :role "DIRECTOR")'

  "entity.create":
    short: "Create a natural person"
    dsl: '(entity.create :entity-type "proper-person" :first-name "John" :last-name "Smith" :date-of-birth "1980-01-15" :nationality "GB" :as @john)'

  "entity.ensure":
    short: "Create or update a UK company"
    dsl: '(entity.ensure :entity-type "limited-company" :name "Holdings Ltd" :jurisdiction "GB" :company-number "12345678" :as @company)'

  "ubo.add-ownership":
    short: "Record 60% ownership"
    dsl: '(ubo.add-ownership :owner-entity-id @person :owned-entity-id @company :percentage 60 :ownership-type "DIRECT")'

  "ubo.trace-chains":
    short: "Trace all ownership to natural persons"
    dsl: "(ubo.trace-chains :cbu-id @fund)"

  "document.catalog":
    short: "Catalog a passport document"
    dsl: '(document.catalog :cbu-id @fund :entity-id @john :doc-type "PASSPORT" :title "John Smith Passport")'

  "product.list":
    short: "List governed products in the catalog"
    dsl: '(product.list :is-active true)'

  "service.list-by-product":
    short: "List services attached to a product"
    dsl: '(service.list-by-product :product-id @product)'

  "service-resource.list-by-service":
    short: "List servicing resources required by a service"
    dsl: '(service-resource.list-by-service :service-id @service)'

  "attribute.list":
    short: "List resource dictionary attributes"
    dsl: '(attribute.list :category "resource")'

  "kyc-case.create":
    short: "Create new client KYC case"
    dsl: '(kyc-case.create :cbu-id @fund :case-type "NEW_CLIENT" :as @case)'

  "screening.run":
    short: "Run PEP screening on entity"
    dsl: '(screening.run :workstream-id @ws :screening-type "PEP")'

  "graph.view":
    short: "View CBU graph in KYC mode"
    dsl: '(graph.view :cbu-id @fund :view-mode "KYC_UBO")'

  "cbu-custody.add-universe":
    short: "Add US equities to trading universe"
    dsl: '(cbu-custody.add-universe :cbu-id @fund :instrument-class "EQUITY" :market "XNYS" :currencies ["USD"] :settlement-types ["DVP"])'

  "service-resource.provision":
    short: "Provision a runtime resource instance for a CBU"
    dsl: '(service-resource.provision :cbu-id @fund :resource-type "CUSTODY_ACCT" :instance-url "https://custody.bank.com/12345")'

  # ---------------------------------------------------------------------------
  # GLEIF RESEARCH EXAMPLES
  # ---------------------------------------------------------------------------
  # Single deterministic lookups - each does ONE thing reliably.

  "gleif.get-umbrella":
    short: "Get umbrella fund for a sub-fund (returns null for SICAVs)"
    dsl: '(gleif.get-umbrella :lei "549300EXAMPLE123456" :as @umbrella)'
    context: "SICAVs are self-governed and have NO umbrella above them. If this returns null, use get-manager instead."

  "gleif.get-manager":
    short: "Get fund manager (ManCo/AIFM) for a fund"
    dsl: "(gleif.get-manager :entity-id @fund :as @manco)"
    context: "For SICAVs, this is your starting point for ownership tracing since they have no umbrella."

  "gleif.get-master-fund":
    short: "Get master fund for a feeder fund"
    dsl: '(gleif.get-master-fund :lei "549300FEEDER789012" :as @master)'

  "gleif.lookup-by-isin":
    short: "Find entity by ISIN (security issuer lookup)"
    dsl: '(gleif.lookup-by-isin :isin "LU0123456789" :as @issuer)'

  # ---------------------------------------------------------------------------
  # TRADING MATRIX EXAMPLES
  # ---------------------------------------------------------------------------
  "investment-manager.assign":
    short: "Assign IM for US equities"
    dsl: |
      (investment-manager.assign
        :cbu-id @fund
        :im-entity-id @blackrock
        :scope {:instrument-class "EQUITY" :market "XNYS" :currency "USD"}
        :priority 10
        :instruction-method "FIX"
        :can-trade true
        :can-settle true)

  "investment-manager.find-for-trade":
    short: "Find IM for a specific trade"
    dsl: |
      (investment-manager.find-for-trade
        :cbu-id @fund
        :instrument-class "EQUITY"
        :market "XNYS"
        :currency "USD")

  "pricing-config.set":
    short: "Configure Bloomberg pricing for equities"
    dsl: |
      (pricing-config.set
        :cbu-id @fund
        :instrument-class "EQUITY"
        :market "XNYS"
        :source "BLOOMBERG"
        :price-type "CLOSING"
        :fallback-source "REUTERS"
        :stale-threshold-hours 24
        :stale-action "USE_FALLBACK")

  "sla.commit":
    short: "Commit to T+1 settlement SLA"
    dsl: |
      (sla.commit
        :cbu-id @fund
        :template-code "TRADE_SETTLEMENT_T1"
        :scope {:instrument-class "EQUITY" :market "XNYS"}
        :effective-date "2024-01-01")

  "sla.record-measurement":
    short: "Record daily settlement rate"
    dsl: |
      (sla.record-measurement
        :commitment-id @sla
        :metric-code "SETTLEMENT_RATE"
        :value 99.7
        :measurement-date "2024-12-30")

  "sla.report-breach":
    short: "Report an SLA breach"
    dsl: |
      (sla.report-breach
        :commitment-id @sla
        :metric-code "SETTLEMENT_RATE"
        :target-value 99.5
        :actual-value 98.2
        :breach-date "2024-12-30"
        :severity "WARNING"
        :root-cause-category "VENDOR")

  "cash-sweep.configure":
    short: "Configure USD cash sweep to STIF"
    dsl: |
      (cash-sweep.configure
        :cbu-id @fund
        :currency "USD"
        :vehicle "STIF"
        :threshold 100000
        :sweep-time "16:00"
        :sweep-timezone "America/New_York"
        :frequency "DAILY")

  "service-resource.analyze-lifecycle-gaps":
    short: "Find missing resources for trading"
    dsl: |
      (service-resource.analyze-lifecycle-gaps
        :cbu-id @fund
        :instrument-class "EQUITY"
        :market "XNYS")

  "service-resource.provision-lifecycle":
    short: "Provision missing lifecycle resources"
    dsl: |
      (service-resource.provision-lifecycle
        :cbu-id @fund
        :lifecycle-id @lifecycle
        :resources ["SSI" "BOOKING_RULE" "IM_CONNECTIVITY"])

  "matrix-overlay.effective-matrix":
    short: "See combined trading matrix for an instrument"
    dsl: |
      (matrix-overlay.effective-matrix
        :cbu-id @fund
        :instrument-class "EQUITY"
        :market "XNYS"
        :currency "USD")
