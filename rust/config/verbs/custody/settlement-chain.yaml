domains:
  settlement-chain:
    description: "Settlement chain configuration - multi-hop settlement paths and cross-border routing"

    verbs:
      # =========================================================================
      # SETTLEMENT LOCATION REFERENCE DATA
      # =========================================================================

      define-location:
        description: "Define settlement location (CSD/ICSD)"
        invocation_phrases:
          - "define settlement location"
          - "add settlement location"
          - "create CSD"
          - "add ICSD"
          - "register settlement venue"
          - "define CSD location"
          - "create settlement depot"
          - "add custodian location"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [trading]
          side_effects: state_write
          harm_class: reversible
        behavior: crud
        crud:
          operation: upsert
          table: settlement_locations
          schema: ob-poc
          conflict_keys:
            - location_code
          returning: location_id
        args:
          - name: code
            type: string
            required: true
            maps_to: location_code
          - name: name
            type: string
            required: true
            maps_to: location_name
          - name: location-type
            type: string
            required: true
            maps_to: location_type
            valid_values:
              - CSD
              - ICSD
              - CUSTODIAN
          - name: country-code
            type: string
            required: false
            maps_to: country_code
          - name: bic
            type: string
            required: false
            maps_to: bic
          - name: operating-hours
            type: json
            required: false
            maps_to: operating_hours
          - name: settlement-cycles
            type: json
            required: false
            maps_to: settlement_cycles
        returns:
          type: uuid
          name: location_id
          capture: true
        lifecycle:
          precondition_checks:
            - "reviewed:none"
          writes_tables:
            - custody.settlement_locations
        three_axis:
          state_effect: preserving
          external_effects: []
          consequence:
            baseline: reviewable

      list-locations:
        description: "List settlement locations"
        invocation_phrases:
          - "list settlement locations"
          - "show CSDs"
          - "view settlement venues"
          - "list custodian locations"
          - "show ICSDs"
          - "display settlement depots"
          - "list all locations"
        metadata:
          tier: reference
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [trading]
          side_effects: facts_only
          harm_class: read_only
        behavior: crud
        crud:
          operation: select
          table: settlement_locations
          schema: ob-poc
        args:
          - name: location-type
            type: string
            required: false
            maps_to: location_type
          - name: country-code
            type: string
            required: false
            maps_to: country_code
        returns:
          type: record_set
        lifecycle:
          reads_tables:
            - custody.settlement_locations

      # =========================================================================
      # SETTLEMENT CHAINS
      # =========================================================================
        three_axis:
          state_effect: preserving
          external_effects: [observational]
          consequence:
            baseline: benign

      create-chain:
        description: "Create settlement chain for CBU"
        invocation_phrases:
          - "create settlement chain"
          - "add settlement chain"
          - "define settlement path"
          - "create settlement route"
          - "new settlement chain"
          - "set up settlement chain"
          - "add settlement configuration"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [trading]
          side_effects: state_write
          harm_class: reversible
        behavior: crud
        crud:
          operation: upsert
          table: cbu_settlement_chains
          schema: ob-poc
          conflict_keys:
            - cbu_id
            - chain_name
          returning: chain_id
        args:
          - name: cbu-id
            type: uuid
            required: true
            maps_to: cbu_id
            lookup:
              table: cbus
              entity_type: cbu
              schema: ob-poc
              search_key: name
              primary_key: cbu_id
          - name: name
            type: string
            required: true
            maps_to: chain_name
          - name: market
            type: lookup
            required: false
            maps_to: market_id
            lookup:
              table: markets
              entity_type: market
              schema: ob-poc
              code_column: mic
              id_column: market_id
          - name: instrument-class
            type: lookup
            required: false
            maps_to: instrument_class_id
            lookup:
              table: instrument_classes
              entity_type: instrument_class
              schema: ob-poc
              code_column: code
              id_column: class_id
          - name: currency
            type: string
            required: false
            maps_to: currency
          - name: settlement-type
            type: string
            required: false
            maps_to: settlement_type
          - name: is-default
            type: boolean
            required: false
            maps_to: is_default
            default: false
          - name: effective-date
            type: date
            required: false
            maps_to: effective_date
          - name: notes
            type: string
            required: false
            maps_to: notes
        returns:
          type: uuid
          name: chain_id
          capture: true
        lifecycle:
          precondition_checks:
            - "requires_scope:cbu"
          writes_tables:
            - custody.cbu_settlement_chains
        three_axis:
          state_effect: preserving
          external_effects: []
          consequence:
            baseline: reviewable

      list-chains:
        description: "List settlement chains for CBU"
        invocation_phrases:
          - "list settlement chains"
          - "show settlement chains"
          - "view settlement paths"
          - "display settlement routes"
          - "list CBU chains"
          - "show settlement configuration"
        metadata:
          tier: reference
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [trading]
          side_effects: facts_only
          harm_class: read_only
        behavior: crud
        crud:
          operation: list_by_fk
          table: cbu_settlement_chains
          schema: ob-poc
          fk_col: cbu_id
        args:
          - name: cbu-id
            type: uuid
            required: true
            lookup:
              table: cbus
              entity_type: cbu
              schema: ob-poc
              search_key: name
              primary_key: cbu_id
          - name: market
            type: string
            required: false
          - name: is-active
            type: boolean
            required: false
            maps_to: is_active
        returns:
          type: record_set
        lifecycle:
          reads_tables:
            - custody.cbu_settlement_chains
        three_axis:
          state_effect: preserving
          external_effects: [observational]
          consequence:
            baseline: benign

      deactivate-chain:
        description: "Deactivate a settlement chain"
        invocation_phrases:
          - "deactivate settlement chain"
          - "disable settlement chain"
          - "turn off settlement chain"
          - "remove settlement chain"
          - "deactivate chain"
          - "end settlement chain"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [trading]
          side_effects: state_write
          harm_class: irreversible
        behavior: crud
        crud:
          operation: update
          table: cbu_settlement_chains
          schema: ob-poc
          key: chain_id
          set_values:
            is_active: false
        args:
          - name: chain-id
            type: uuid
            required: true
            maps_to: chain_id
        returns:
          type: affected
        lifecycle:
          precondition_checks:
            - "requires_scope:cbu"
          writes_tables:
            - custody.cbu_settlement_chains

      # =========================================================================
      # CHAIN HOPS (INTERMEDIARIES)
      # =========================================================================
        three_axis:
          state_effect: transition
          external_effects: []
          consequence:
            baseline: requires_confirmation
        transition_args:
          entity_id_arg: chain-id
          target_workspace: instrument_matrix
          target_slot: settlement_pattern_template

      add-hop:
        description: "Add intermediary hop to settlement chain"
        invocation_phrases:
          - "add settlement hop"
          - "add intermediary"
          - "add chain hop"
          - "insert settlement step"
          - "add subcustodian hop"
          - "add agent hop"
          - "add settlement intermediary"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [trading]
          side_effects: state_write
          harm_class: reversible
        behavior: crud
        crud:
          operation: upsert
          table: settlement_chain_hops
          schema: ob-poc
          conflict_keys:
            - chain_id
            - hop_sequence
          returning: hop_id
        args:
          - name: chain-id
            type: uuid
            required: true
            maps_to: chain_id
          - name: sequence
            type: integer
            required: true
            maps_to: hop_sequence
          - name: role
            type: string
            required: true
            maps_to: role
            valid_values:
              - CUSTODIAN
              - SUBCUSTODIAN
              - AGENT
              - CSD
              - ICSD
          - name: intermediary-entity-id
            type: uuid
            required: false
            maps_to: intermediary_entity_id
            lookup:
              table: entities
              entity_type: entity
              schema: ob-poc
              search_key: name
              primary_key: entity_id
          - name: intermediary-bic
            type: string
            required: false
            maps_to: intermediary_bic
          - name: intermediary-name
            type: string
            required: false
            maps_to: intermediary_name
          - name: account-number
            type: string
            required: false
            maps_to: account_number
          - name: ssi-id
            type: uuid
            required: false
            maps_to: ssi_id
            lookup:
              table: cbu_ssi
              entity_type: ssi
              schema: ob-poc
              search_key: ssi_name
              primary_key: ssi_id
          - name: instructions
            type: string
            required: false
            maps_to: instructions
        returns:
          type: uuid
          name: hop_id
          capture: true
        lifecycle:
          precondition_checks:
            - "requires_scope:cbu"
            - "requires_prior:settlement-chain.create-chain"
          writes_tables:
            - custody.settlement_chain_hops
          reads_tables:
            - custody.cbu_settlement_chains
        three_axis:
          state_effect: transition
          external_effects: []
          consequence:
            baseline: reviewable
        transition_args:
          entity_id_arg: chain-id
          target_workspace: instrument_matrix
          target_slot: settlement_pattern_template

      list-hops:
        description: "List hops in a settlement chain"
        invocation_phrases:
          - "list settlement hops"
          - "show chain hops"
          - "view intermediaries"
          - "display settlement steps"
          - "list chain intermediaries"
          - "show settlement path"
        metadata:
          tier: reference
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [trading]
          side_effects: facts_only
          harm_class: read_only
        behavior: crud
        crud:
          operation: list_by_fk
          table: settlement_chain_hops
          schema: ob-poc
          fk_col: chain_id
          order_by: hop_sequence
        args:
          - name: chain-id
            type: uuid
            required: true
            maps_to: chain_id
        returns:
          type: record_set
        lifecycle:
          reads_tables:
            - custody.settlement_chain_hops
        three_axis:
          state_effect: preserving
          external_effects: [observational]
          consequence:
            baseline: benign

      remove-hop:
        description: "Remove hop from settlement chain"
        invocation_phrases:
          - "remove settlement hop"
          - "delete chain hop"
          - "remove intermediary"
          - "delete settlement step"
          - "remove hop from chain"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [trading]
          side_effects: state_write
          harm_class: destructive
        behavior: crud
        crud:
          operation: delete
          table: settlement_chain_hops
          schema: ob-poc
        args:
          - name: hop-id
            type: uuid
            required: true
            maps_to: hop_id
        returns:
          type: affected
        lifecycle:
          precondition_checks:
            - "requires_scope:cbu"
            - "requires_prior:settlement-chain.create-chain"
          writes_tables:
            - custody.settlement_chain_hops

      # =========================================================================
      # SETTLEMENT LOCATION PREFERENCES
      # =========================================================================
        three_axis:
          state_effect: preserving
          external_effects: []
          consequence:
            baseline: requires_confirmation

      set-location-preference:
        description: "Set preferred settlement location for market/instrument"
        invocation_phrases:
          - "set settlement preference"
          - "set location preference"
          - "prefer settlement location"
          - "set preferred CSD"
          - "configure settlement preference"
          - "set default settlement"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [trading]
          side_effects: state_write
          harm_class: reversible
        behavior: crud
        crud:
          operation: upsert
          table: cbu_settlement_location_preferences
          schema: ob-poc
          conflict_keys:
            - cbu_id
            - market_id
            - instrument_class_id
            - preferred_location_id
          returning: preference_id
        args:
          - name: cbu-id
            type: uuid
            required: true
            maps_to: cbu_id
            lookup:
              table: cbus
              entity_type: cbu
              schema: ob-poc
              search_key: name
              primary_key: cbu_id
          - name: market
            type: lookup
            required: false
            maps_to: market_id
            lookup:
              table: markets
              entity_type: market
              schema: ob-poc
              code_column: mic
              id_column: market_id
          - name: instrument-class
            type: lookup
            required: false
            maps_to: instrument_class_id
            lookup:
              table: instrument_classes
              entity_type: instrument_class
              schema: ob-poc
              code_column: code
              id_column: class_id
          - name: location-id
            type: uuid
            required: true
            maps_to: preferred_location_id
            lookup:
              table: settlement_locations
              entity_type: settlement_location
              schema: ob-poc
              search_key: location_code
              primary_key: location_id
          - name: priority
            type: integer
            required: false
            maps_to: priority
            default: 50
          - name: reason
            type: string
            required: false
            maps_to: reason
        returns:
          type: uuid
          name: preference_id
          capture: true
        lifecycle:
          precondition_checks:
            - "requires_scope:cbu"
          writes_tables:
            - custody.cbu_settlement_location_preferences
          reads_tables:
            - custody.settlement_locations
        three_axis:
          state_effect: preserving
          external_effects: []
          consequence:
            baseline: reviewable

      list-location-preferences:
        description: "List settlement location preferences for CBU"
        invocation_phrases:
          - "list settlement preferences"
          - "show location preferences"
          - "view settlement preferences"
          - "list preferred locations"
          - "show settlement choices"
        metadata:
          tier: reference
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [trading]
          side_effects: facts_only
          harm_class: read_only
        behavior: crud
        crud:
          operation: list_by_fk
          table: cbu_settlement_location_preferences
          schema: ob-poc
          fk_col: cbu_id
          order_by: priority
        args:
          - name: cbu-id
            type: uuid
            required: true
            lookup:
              table: cbus
              entity_type: cbu
              schema: ob-poc
              search_key: name
              primary_key: cbu_id
        returns:
          type: record_set
        lifecycle:
          reads_tables:
            - custody.cbu_settlement_location_preferences

      # =========================================================================
      # CROSS-BORDER CONFIGURATION
      # =========================================================================
        three_axis:
          state_effect: preserving
          external_effects: [observational]
          consequence:
            baseline: benign

      set-cross-border:
        description: "Configure cross-border settlement routing"
        invocation_phrases:
          - "set cross-border routing"
          - "configure cross-border"
          - "add cross-border settlement"
          - "set international settlement"
          - "configure bridge settlement"
          - "set cross-border config"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [trading]
          side_effects: state_write
          harm_class: reversible
        behavior: crud
        crud:
          operation: upsert
          table: cbu_cross_border_config
          schema: ob-poc
          conflict_keys:
            - cbu_id
            - source_market_id
            - target_market_id
          returning: config_id
        args:
          - name: cbu-id
            type: uuid
            required: true
            maps_to: cbu_id
            lookup:
              table: cbus
              entity_type: cbu
              schema: ob-poc
              search_key: name
              primary_key: cbu_id
          - name: source-market
            type: lookup
            required: true
            maps_to: source_market_id
            lookup:
              table: markets
              entity_type: market
              schema: ob-poc
              code_column: mic
              id_column: market_id
          - name: target-market
            type: lookup
            required: true
            maps_to: target_market_id
            lookup:
              table: markets
              entity_type: market
              schema: ob-poc
              code_column: mic
              id_column: market_id
          - name: settlement-method
            type: string
            required: true
            maps_to: settlement_method
            valid_values:
              - BRIDGE
              - DIRECT
              - VIA_ICSD
          - name: bridge-location-id
            type: uuid
            required: false
            maps_to: bridge_location_id
            lookup:
              table: settlement_locations
              entity_type: settlement_location
              schema: ob-poc
              search_key: location_code
              primary_key: location_id
          - name: preferred-currency
            type: string
            required: false
            maps_to: preferred_currency
          - name: fx-timing
            type: string
            required: false
            maps_to: fx_timing
            valid_values:
              - PRE_SETTLEMENT
              - ON_SETTLEMENT
              - POST_SETTLEMENT
          - name: additional-days
            type: integer
            required: false
            maps_to: additional_days
            default: 0
          - name: special-instructions
            type: string
            required: false
            maps_to: special_instructions
        returns:
          type: uuid
          name: config_id
          capture: true
        lifecycle:
          precondition_checks:
            - "requires_scope:cbu"
          writes_tables:
            - custody.cbu_cross_border_config
          reads_tables:
            - custody.settlement_locations
        three_axis:
          state_effect: preserving
          external_effects: []
          consequence:
            baseline: reviewable

      list-cross-border:
        description: "List cross-border configurations for CBU"
        invocation_phrases:
          - "list cross-border configs"
          - "show cross-border routing"
          - "view international settlement"
          - "list cross-border"
          - "show bridge configurations"
        metadata:
          tier: reference
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [trading]
          side_effects: facts_only
          harm_class: read_only
        behavior: crud
        crud:
          operation: list_by_fk
          table: cbu_cross_border_config
          schema: ob-poc
          fk_col: cbu_id
        args:
          - name: cbu-id
            type: uuid
            required: true
            lookup:
              table: cbus
              entity_type: cbu
              schema: ob-poc
              search_key: name
              primary_key: cbu_id
          - name: is-active
            type: boolean
            required: false
            maps_to: is_active
        returns:
          type: record_set
        lifecycle:
          reads_tables:
            - custody.cbu_cross_border_config
        three_axis:
          state_effect: preserving
          external_effects: [observational]
          consequence:
            baseline: benign

      deactivate-cross-border:
        description: "Deactivate cross-border configuration"
        invocation_phrases:
          - "deactivate cross-border"
          - "disable cross-border routing"
          - "turn off cross-border"
          - "remove cross-border config"
          - "end cross-border"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [trading]
          side_effects: state_write
          harm_class: irreversible
        behavior: crud
        crud:
          operation: update
          table: cbu_cross_border_config
          schema: ob-poc
          key: config_id
          set_values:
            is_active: false
        args:
          - name: config-id
            type: uuid
            required: true
            maps_to: config_id
        returns:
          type: affected
        lifecycle:
          precondition_checks:
            - "requires_scope:cbu"
          writes_tables:
            - custody.cbu_cross_border_config

      # =========================================================================
      # P.3 ADDITIONS (2026-04-23) — SETTLEMENT CHAIN LIFECYCLE TRANSITIONS
      # Adds verbs for the 6-state settlement_pattern_lifecycle per
      # instrument_matrix_dag.yaml. Implements Adam's pre-activation
      # lifecycle (pass-1 Q1) + parallel_run state (pass-5 Q-E).
      # =========================================================================
        three_axis:
          state_effect: preserving
          external_effects: []
          consequence:
            baseline: requires_confirmation

      request-review:
        description: "Submit a configured settlement chain for ops / treasury review"
        invocation_phrases:
          - "request review"
          - "submit chain for review"
          - "submit settlement chain"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [im_configured]
          side_effects: state_write
          harm_class: reversible
        behavior: plugin
        args:
          - name: chain
            type: uuid
            required: true
        three_axis:
          state_effect: transition
          external_effects: []
          consequence:
            baseline: reviewable
          transitions:
            dag: instrument_matrix_dag
            edges:
              - from: configured
                to: reviewed
        transition_args:
          entity_id_arg: chain
          target_workspace: instrument_matrix
          target_slot: settlement_pattern_template

      enter-parallel-run:
        description: "Enter parallel-run state — new chain runs alongside incumbent for 1-2 cycles"
        invocation_phrases:
          - "enter parallel run"
          - "start parallel settlement"
          - "shadow settlement"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [parallel_run]
          side_effects: state_write
          harm_class: reversible
        behavior: plugin
        args:
          - name: chain
            type: uuid
            required: true
        three_axis:
          state_effect: transition
          external_effects: [emitting]
          consequence:
            baseline: requires_confirmation
          transitions:
            dag: instrument_matrix_dag
            edges:
              - from: reviewed
                to: parallel_run

        transition_args:
          entity_id_arg: chain
          target_workspace: instrument_matrix
          target_slot: settlement_pattern_template
      go-live:
        description: "Activate chain as live (parallel_run → live)"
        invocation_phrases:
          - "go live on chain"
          - "activate chain"
          - "make settlement chain live"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [active]
          side_effects: state_write
          harm_class: reversible
        behavior: plugin
        args:
          - name: chain
            type: uuid
            required: true
        three_axis:
          state_effect: transition
          external_effects: [emitting]
          consequence:
            baseline: requires_confirmation
          transitions:
            dag: instrument_matrix_dag
            edges:
              - from: parallel_run
                to: live
        transition_args:
          entity_id_arg: chain
          target_workspace: instrument_matrix
          target_slot: settlement_pattern_template

      abort-parallel-run:
        description: "Abort parallel_run — roll back to reviewed state (issues found)"
        invocation_phrases:
          - "abort parallel run"
          - "rollback parallel chain"
          - "stop parallel settlement"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [operational]
          side_effects: state_write
          harm_class: reversible
        behavior: plugin
        args:
          - name: chain
            type: uuid
            required: true
          - name: reason
            type: string
            required: false
        three_axis:
          state_effect: transition
          external_effects: [emitting]
          consequence:
            baseline: requires_confirmation
          transitions:
            dag: instrument_matrix_dag
            edges:
              - from: parallel_run
                to: reviewed

        transition_args:
          entity_id_arg: chain
          target_workspace: instrument_matrix
          target_slot: settlement_pattern_template
      suspend:
        description: "Suspend a live settlement chain (operational hold)"
        invocation_phrases:
          - "suspend chain"
          - "suspend settlement chain"
          - "pause chain"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [operational]
          side_effects: state_write
          harm_class: reversible
        behavior: plugin
        args:
          - name: chain
            type: uuid
            required: true
          - name: reason
            type: string
            required: false
        three_axis:
          state_effect: transition
          external_effects: [emitting]
          consequence:
            baseline: requires_confirmation
          transitions:
            dag: instrument_matrix_dag
            edges:
              - from: live
                to: suspended
        transition_args:
          entity_id_arg: chain
          target_workspace: instrument_matrix
          target_slot: settlement_pattern_template

      reactivate:
        description: "Reactivate a suspended settlement chain (suspended → live)"
        invocation_phrases:
          - "reactivate chain"
          - "resume settlement chain"
        metadata:
          tier: intent
          source_of_truth: matrix
          scope: cbu
          noun: settlement_chain
          subject_kinds: [settlement-chain]
          phase_tags: [operational]
          side_effects: state_write
          harm_class: reversible
        behavior: plugin
        args:
          - name: chain
            type: uuid
            required: true
        three_axis:
          state_effect: transition
          external_effects: [emitting]
          consequence:
            baseline: requires_confirmation
          transitions:
            dag: instrument_matrix_dag
            edges:
              - from: suspended
                to: live
        transition_args:
          entity_id_arg: chain
          target_workspace: instrument_matrix
          target_slot: settlement_pattern_template
