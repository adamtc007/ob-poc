[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
rust-version = "1.84"

# xtask is a convention for project-specific automation
# See: https://github.com/matklad/cargo-xtask

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
serde_yaml = "0.9"
