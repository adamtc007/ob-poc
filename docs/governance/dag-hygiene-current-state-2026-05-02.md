# DAG Hygiene Current-State Inventory - 2026-05-02

> **Generated by:** Codex implementation pass, Phase 0.b.
> **Plan:** `docs/todo/DAG_REACHABILITY_SEMOS_HYGIENE_PLAN_2026-05-02.md`.
> **Mode:** Read/inventory only; no behavior code changed.

## Evidence and Commands

Evidence files now live in the repo:

- `docs/governance/dag-reachability-audit-2026-05-02.md`
- `docs/governance/dag-reachability-semos-reconciliation-2026-05-02.md`
- `docs/todo/P0_dag_reachability_remediation.md`

Cargo commands must be run from `rust/`.

Confirmed command surface:

- `cargo run -p xtask -- schema-export`
- `cargo run -p xtask -- verbs lint`
- `cargo run -p xtask -- reconcile validate`

`xtask schema-export` currently writes root `schema_export.sql` and root
`migrations/master-schema.sql`.

## Baseline Validation

| Check | Result |
|---|---|
| `env RUSTC_WRAPPER= cargo check` | pass |
| `env RUSTC_WRAPPER= cargo run -p xtask -- reconcile validate` | pass: 139 domains, 1288 verbs, 12 DAG taxonomies, 12 packs, 0 structural/well-formedness/policy errors |
| `env RUSTC_WRAPPER= cargo run -p xtask -- verbs lint` | pass with 67 warnings; no errors |

The default `cargo check` path can fail locally because `sccache` is blocked by the execution environment. The project workaround is `RUSTC_WRAPPER=`.

## Schema Export Drift

Root `migrations/master-schema.sql` is the current export target, but it is stale relative to the intended D-004 deal model:

- `migrations/master-schema.sql` still allows `BAC_APPROVAL` and `KYC_CLEARANCE` in `deals_status_check`.
- `migrations/master-schema.sql` does not show `deals.bac_status` or `deals.kyc_clearance_status`.
- `rust/migrations/20260429_carrier_04_deals_status_in_clearance.sql` contains the intended `IN_CLEARANCE` constraint rewrite.
- `rust/migrations/20260429_carrier_08_deals_in_clearance_substates.sql` contains the intended substate columns.

Conclusion: schema export refresh must happen before validator hard-fail work relies on `migrations/master-schema.sql` as truth.

## SimpleStatus Drift Inventory

### `cbu-ca.*`

Current configs live in `rust/src/domain_ops/simple_status_op.rs:226-270`.

| FQN | Current config | Schema fact | Finding |
|---|---|---|---|
| `cbu-ca.submit-for-review` | `table=cbu_corporate_action_events`, `pk_col=id`, `state_col=status`, `target=under_review` | root schema has `event_id` and `ca_status`; CHECK allows `under_review` | column drift and likely PK drift |
| `cbu-ca.approve` | same, `target=approved` | CHECK allows `approved` | column drift and likely PK drift |
| `cbu-ca.reject` | same, `target=rejected` | CHECK allows `rejected` | column drift and likely PK drift |
| `cbu-ca.withdraw` | same, `target=withdrawn` | CHECK allows `withdrawn` | column drift and likely PK drift |
| `cbu-ca.mark-implemented` | same, `target=implemented` | CHECK allows `implemented` | column drift and likely PK drift |

DAG references exist in `rust/config/sem_os_seeds/dag_taxonomies/cbu_dag.yaml:948-963`.
YAML declarations exist in `rust/config/verbs/cbu-ca.yaml`.

Remediation shape: repair in place. Do not rip.

### Deal BAC/substate verbs

Current configs live in `rust/src/domain_ops/simple_status_op.rs:272-299`.

| FQN | Current SimpleStatus behavior | YAML intent | Finding |
|---|---|---|---|
| `deal.submit-for-bac` | writes `deals.deal_status = BAC_APPROVAL` | description says writes `deal_status=IN_CLEARANCE` and `bac_status=in_review` | runtime/YAML semantic split |
| `deal.bac-approve` | writes `deals.deal_status = KYC_CLEARANCE` | description says writes `deals.bac_status=approved` | runtime/YAML semantic split |
| `deal.bac-reject` | writes `deals.deal_status = REJECTED` | description says writes `deals.bac_status=rejected`, with terminal behavior conditional | runtime/YAML semantic split |

DAG references exist in `rust/config/sem_os_seeds/dag_taxonomies/deal_dag.yaml:149-151` and `deal_dag.yaml:382`.
YAML declarations exist in `rust/config/verbs/deal.yaml:2124-2238`.

Remediation shape: rip the three SimpleStatus registrations and replace with dedicated plugin ops in one commit.

### `service-intent.suspend/resume/cancel`

Current configs live in `rust/src/domain_ops/simple_status_op.rs:793-819`.

| FQN | Current config | YAML file | DAG references | Finding |
|---|---|---|---|---|
| `service-intent.suspend` | SimpleStatus on `cbu_service_intent.status -> suspended` | `rust/config/verbs/service-pipeline.yaml:161` | `instrument_matrix_dag.yaml:828` | not YAML-only in current repo |
| `service-intent.resume` | SimpleStatus on `cbu_service_intent.status -> active` | `rust/config/verbs/service-pipeline.yaml:202` | `instrument_matrix_dag.yaml:831` | not YAML-only in current repo |
| `service-intent.cancel` | SimpleStatus on `cbu_service_intent.status -> cancelled` | `rust/config/verbs/service-pipeline.yaml:237` | `instrument_matrix_dag.yaml:834,837` | not YAML-only in current repo |

Remediation shape: do not rip. Validate whether SimpleStatus routing is sufficient and whether `PendingStateAdvance` is emitted as expected.

### `settlement-chain.*`

Current configs live in `rust/src/domain_ops/simple_status_op.rs:869-922`.

| FQN | Current config | Schema fact | DAG references |
|---|---|---|---|
| `settlement-chain.request-review` | writes `cbu_settlement_chains.status` | table has `is_active`, no `status` | yes |
| `settlement-chain.enter-parallel-run` | writes `cbu_settlement_chains.status` | table has `is_active`, no `status` | yes |
| `settlement-chain.go-live` | writes `cbu_settlement_chains.status` | table has `is_active`, no `status` | yes |
| `settlement-chain.abort-parallel-run` | writes `cbu_settlement_chains.status` | table has `is_active`, no `status` | yes |
| `settlement-chain.suspend` | writes `cbu_settlement_chains.status` | table has `is_active`, no `status` | yes |
| `settlement-chain.reactivate` | writes `cbu_settlement_chains.status` | table has `is_active`, no `status` | yes |

YAML declarations exist in `rust/config/verbs/custody/settlement-chain.yaml:899-1125`.
DAG references exist in `rust/config/sem_os_seeds/dag_taxonomies/instrument_matrix_dag.yaml:392-407`.

Remediation shape: decision required. Ripping only the configs would leave DAG references dangling.

### `trading-profile.*` drift trio

Current configs live in `rust/src/domain_ops/simple_status_op.rs:943-980`.

| FQN | Current target | Schema CHECK allows | DAG references |
|---|---|---|---|
| `trading-profile.enter-parallel-run` | `PARALLEL_RUN` | `DRAFT`, `VALIDATED`, `PENDING_REVIEW`, `ACTIVE`, `SUPERSEDED`, `ARCHIVED` | yes |
| `trading-profile.abort-parallel-run` | `APPROVED` | same | yes |
| `trading-profile.suspend` | `SUSPENDED` | same | yes |

YAML declarations exist in `rust/config/verbs/trading-profile.yaml:1822-1940`.
DAG references exist in `rust/config/sem_os_seeds/dag_taxonomies/instrument_matrix_dag.yaml:144,173,192,619,625,628`.

Remediation shape: decision required. Either schema/DAG should support these states, or the DAG and verbs should be retired together.

## Cascade Child Verb Observations

Current repo facts found during inventory:

- `delivery.start` exists as a SimpleStatus config.
- `service-resource.provision` exists as a registered plugin op.
- `kyc-case.update-status` exists as a registered plugin op.
- `capital.issue-shares` exists and may be the child verb for `capital.adjust-holding`.
- client-group removal appears to be `client-group.entity-remove`, not `client-group.remove-entity`.
- No confirmed current FQN found for `service-intent.activate`, `trading-profile.clone-from-template`, `entity.deactivate`, or `cbu-role.terminate`.

Conclusion: Phase 5 needs the planned child-verb gap packet before cascade refactors.

## Phase 0 Conclusions

1. The approved plan's ordering is still valid.
2. Phase 2.a can proceed as an in-place mechanical repair.
3. Phase 2.b can proceed as a coupled rip-and-replace, but must include `deal.bac-reject`.
4. Settlement-chain/trading-profile rips remain blocked on a decision packet because they are still DAG-referenced.
5. Schema export refresh is required before schema-driven validators become authoritative.
