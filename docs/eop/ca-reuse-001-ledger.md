# EOP-PLAN-CA-REUSE-001 — ledger (ob-poc)

- **Prompt C (ob-poc rewire)** — 2026-09-09 — GREEN. Rebased onto dsl repo tag `eop/ca-reuse-001-dsl` (`5925702ecbc709204181999d1add842e4d66e23f`): 4 relocated crates (not 3) repointed to git deps, 9 downstream `Cargo.toml`s repointed, local `[patch]` removed, 8 API consumers (not 5) + 4 test files (9 call sites) adapted to the new `KindCatalogue` API; found+fixed 2 real regressions beyond the itemised scope (`dsl-source/verbs/registry/movement.dsl` stale float-literal drift under DSL-T2's decimal rejection; `config/semantic-packs.lock` stale `compiler_version`/hash receipts); found+wired 14 more `ContextResolutionRequest` sites and 5 more `DomainTransition`/`SourceOfTruth` sites the recon's grep missed; declared `slot_path`/`node_prefix` on both KYC domain-pack transitions (FU-T2-02, byte-identical dry-run output verified); renamed `KycPreconditionFailure`→`PreconditionFailure`/`kyc_manifest_hash`→`lexicon_manifest_hash` across 6 sites incl. 3 the recon didn't name; assessed and explicitly declined to decouple `ExecutionPath`/`EnvelopeHandle` from `ob-poc-types` (real design decision, not mechanical — reported, not absorbed); removed 4 orphaned `audits/surface` baselines. Full workspace suite green; every remaining red test traced to pre-existing, uncommitted, unrelated dirty-tree state (`config/slot_state_table.yaml`, 2 deleted custody verbs) or long-standing content drift predating this tranche, confirmed via a disposable `git worktree` diff against the `5be3bbf3` base commit. Receipt: `docs/eop/EOP-PLAN-CA-REUSE-001_Prompt-C-ob-poc-rewire_v0.1.md`. Branch `eop/ca-reuse-001`, not pushed.
